# Required
SECRET_KEY=your-secret-key-here-change-in-production

# MongoDB (optional — falls back to mock data if unavailable)
MONGO_URI=mongodb://localhost:27017
DB_NAME=luminastay

# Server
HOST=0.0.0.0
PORT=8000
